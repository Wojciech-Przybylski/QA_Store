pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "sudo apt install -y python3-pip"
            }
        }
        stage('Dependencies') {
            steps {
                sh "pip install -r App/requirements.txt"
            }
        }
        stage('Deploy') {
            steps {
                sh "python3 App/app.py"
            }
        }
    }
}